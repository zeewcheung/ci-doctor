# Local reproduction guidance

> FICTIONAL LOCAL DEMO / SYNTHETIC ADVERSARIAL BENCHMARK — local-only evaluation, not customer data, production evidence, or a general accuracy claim.

Create `dist/fictional-package.tgz` in an isolated fixture copy and compare it with the workflow's declared artifact glob. This validates local file matching only and does not trigger an upload.
